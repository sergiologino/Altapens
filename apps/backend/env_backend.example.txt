# Скопируйте в env_backend.txt или задайте переменные в Run Configuration IDEA (не коммитьте секреты).
# См. RUN_LOCAL.md

DB_URL=jdbc:postgresql://localhost:5432/altacare
DB_USERNAME=postgres
DB_PASSWORD=postgres
JWT_SECRET=local-dev-secret-at-least-32-chars-long!!
SERVER_PORT=8080

AI_INTEGRATION_BASE_URL=http://localhost:8091
AI_INTEGRATION_API_KEY=your_client_x_api_key_from_noteapp
# Необязательно: имя нейросети speech_synthesis в интеграции (напр. openai-tts); иначе — автовыбор по типу.
# AI_INTEGRATION_DEFAULT_SPEECH_NETWORK=openai-tts
# Переопределение голоса/модели в payload к интеграции (ключ OpenAI задаётся только в noteapp-ai-integration).
# ALTAPENS_TTS_VOICE=nova
# ALTAPENS_TTS_MODEL=
